<h1>Liste de tous les utilisateurs de l'application</h1>

<ul>
    <% @users.each do |user| %>
        <li><a href="<%= url_for(:controller => "users", :action => "show", :id => user.id, :only_path => true) %>"><%= user.first_name %> <%= user.last_name %></a></li>
    <% end %>
</ul>